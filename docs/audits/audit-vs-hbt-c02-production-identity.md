# VS-HBT-C02 — Production Content Identity Cleanup Audit

## Status

DONE — waiting audit. This branch changes production content identity only; Meta V5, combat rules, Balance V1 values and legacy save semantics remain unchanged.

## Active equipment

- `lac-viet-bronze-sword` — **Gươm Đồng Lạc Việt**, weapon.
- `lac-viet-swift-jade` — **Ngọc Tốc Chiến**, gem.

Both use the exact existing Lv1–10 Balance V1 modifier tables previously used by `green-dragon-blade` and `swift-jade`. “Gươm Đồng Lạc Việt” is a game identity inspired by period material culture; it is not asserted as a documented personal weapon of a named historical figure.

## Legacy compatibility

`green-dragon-blade` and `swift-jade` definitions remain registered and derive their existing modifiers from the same tables. Existing Meta V5 inventory instances are not rewritten: definition ID, level, equipment ownership and calculated modifiers remain loadable. Legacy definitions are not newly generated by active Gacha or fresh production UI candidates unless the save already owns/equips them.

## Active flow

- Fresh Gold Gacha weapon reward → `lac-viet-bronze-sword` Lv1.
- Fresh Gold Gacha gem reward → `lac-viet-swift-jade` Lv1.
- Merge remains three same definition + same level → one next-level instance; Lv10 remains the cap.
- Active production equipment IDs are explicitly `lac-viet-bronze-sword` and `lac-viet-swift-jade`.
- Stage clear ID is `hbt-lang-bac-stage-01`; reward values remain Gold 20, KNB 1 and Anh Hồn 10.

## Canonical content names

Production imports now prefer `haiBaTrungMap`, `haiBaTrungWaves`, `createHaiBaTrungRewardConfig`, `haiBaTrungGoldGachaConfig` and `haiBaTrungEquipmentV2Definitions`. Compatibility aliases (`prototypeMap`, `prototypeWaves`, `prototypeRewardBalance`, `prototypeGoldGachaConfig`, `prototypeEquipmentV2Definitions`) remain intentionally for existing tests/imports and are not additional truths.

Tam Quốc Heroes, skills and legacy equipment remain stored compatibility content. They are excluded from the active HBT roster, Recruitment, HBT waves, fresh Gacha equipment rewards and fresh visible equipment candidates.

## VS-HBT-C02A final verification

PASS — Balance V1 now exposes one shared weapon table and one shared gem table, with active HBT keys and legacy aliases pointing to the same data. Simulation merge/accounting resolves active IDs normally; no numeric balance changed. Active equipment merge, Lv10 cap, legacy Lv3 modifiers and fresh Gacha ID boundaries are covered by regression tests.

Verification: `npm test` 24 files / 187 tests PASS; `npm run build` PASS; `npm run preview` HTTP 200; `git diff --check` PASS. Branch remains unmerged to `main`.
